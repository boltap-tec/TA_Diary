{
  "name": "ta-diary",
  "version": "1.0.0",
  "description": "Offline TA bill & tour diary web app (India Post field officers)",
  "private": true,
  "scripts": {
    "build": "node scripts/gen-config.js",
    "start": "python -m http.server 8891",
    "copy:web": "node scripts/copy-web.mjs",
    "apk": "node scripts/copy-web.mjs && npx cap copy android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
