scratchpad/
*.log
.DS_Store
Thumbs.db

# local secrets / config (never commit)
config.js
.env
.env.*

# node / capacitor build artifacts
node_modules/
www/
android/local.properties

# built apk (delivered separately, not committed)
*.apk
